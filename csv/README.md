This folder contains lists of objects existing in an ABAP release:

- DDIC Types, Classes, Interfaces (table `DDTYPES`)
- Function Modules (table `TFDIR`)

Note: Namespaced objects are *not* included.